# 普通正文样本

## 二级标题：普通段落

这是一段普通正文，包含**加粗文字**、*斜体文字*、~~删除线~~，以及一段[普通链接](https://example.com/article)。

行内代码 `const answer = 42` 应与普通文本区分，但不算代码块。

### 三级标题：多段落

这是第一段。

这是第二段，中间以空行分隔。两段应被视为独立的内容块。

本样本不含行内公式语法（见 s5-filtered.md）。

本样本不含任务列表语法（见 s2-long-lists.md）。

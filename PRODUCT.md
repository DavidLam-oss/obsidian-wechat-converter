# Product

## Register

product

## Users

本产品服务于在 Obsidian 中完成写作，并需要把内容稳定发布到微信公众号、飞书云文档或其他内容平台的创作者。

核心用户包括：

- 重视代码块、公式、Mermaid、表格、Callout 和本地图片还原度的技术写作者。
- 需要维护公众号账号、封面、摘要、草稿更新和素材的内容运营者。
- 希望把文章同步到飞书，供团队协作、归档与持续更新的知识工作者。
- 需要通过浏览器插件把同一篇文章分发到多个平台草稿箱的多平台创作者。

他们通常已经在 Obsidian 中形成稳定写作习惯，不希望为了发布而重新排版、重复上传媒体，或把平台登录态交给不必要的中间系统。

## Product Purpose

微信公众号排版转换器把 Obsidian 中的 Markdown 笔记转化为可信、可检查、可继续发布的内容。核心工作流是：打开转换器、检查实时预览、调整文章样式、复制富 HTML，或同步到微信公众号、飞书云文档及浏览器发布助手。

产品存在的意义不是替用户创作，而是降低内容从本地知识库进入发布平台时的格式损失、媒体处理和重复操作成本。成功意味着用户在离开 Obsidian 前，已经知道内容会以什么状态到达目标平台，也清楚下一步该做什么。

产品边界：

- 不修改用户的源 Markdown；标点规范化、主题样式和兼容处理只作用于预览、复制与同步输出。
- 不在 Obsidian 插件中托管其他内容平台的登录态；这部分由浏览器插件使用浏览器现有会话处理。
- 不替用户完成第三方平台的最终发布；产品负责准备或保存草稿，最终发布仍由用户确认。
- 不静默覆盖用户明确选择的标题、封面、摘要、账号、目录或发布目标。
- 不用模糊的自动降级掩盖失败；兼容处理必须可预期，并保留可理解的结果或错误提示。

## Brand Personality

**克制、可靠、精致。**

语气应直接、平静、具体，像一位熟悉内容平台限制的出版助手。它不夸大能力，也不向普通用户暴露内部模块、协议或错误码；先说明当前结果与影响，再给出可执行的下一步。

产品应让用户感到内容始终在自己掌控之中：预览可信，设置可理解，发布状态可追踪，失败后可以恢复。

## Anti-references

- 不做营销落地页式的工具界面：拒绝超大标题、装饰性 Hero、漂浮卡片堆叠和为展示功能而存在的说明区。
- 不使用通用 AI 产品套路：拒绝紫蓝渐变、发光色块、玻璃拟态和没有业务含义的装饰动画。
- 不脱离 Obsidian 重新发明基础控件：拒绝陌生的开关、输入框、滚动条和非标准弹窗行为。
- 不用过多嵌套卡片、过大圆角和高饱和色制造层级；发布工具应安静、紧凑、便于扫描。
- 不把文章主题色当作插件 UI 的成功、警告或错误色；内容表达与操作状态必须各自独立。
- 不只显示“成功”或“失败”而隐藏下一步；状态反馈必须说明结果、影响和可继续执行的动作。

## Design Principles

1. **预览就是承诺。** 预览、复制与同步输出应尽可能一致；无法完全一致时，要让差异和平台限制可见。
2. **任务优先于展示。** 界面围绕检查内容、调整样式和完成发布组织，避免与当前任务无关的视觉装饰。
3. **状态必须导向下一步。** 加载、成功、失败、额度与连接状态不仅要可见，还要告诉用户接下来能做什么。
4. **明确尊重用户选择。** 标题、封面、摘要、账号和发布目标等显式输入优先于推导值与默认值。
5. **兼容性与安全是产品能力。** HTML 清洗、链接与路径校验、媒体处理、凭据边界和平台限制不能作为实现细节被弱化。
6. **平台职责清晰。** Obsidian 插件负责内容准备与任务发起，浏览器插件负责浏览器登录态和平台草稿操作，云端服务只承担已声明的必要职责。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为目标，并遵循 Obsidian 的主题与可访问性能力：

- 所有关键操作支持键盘访问，焦点状态清晰且不会只依赖颜色表达。
- 成功、警告、错误、连接与额度状态同时提供文字或图标语义。
- 正文、标签、禁用态和边界在浅色与深色主题下保持足够对比度。
- 设置页、弹窗和移动布局中的文字不截断关键含义，长标题与长路径可以换行或安全省略。
- 动效只用于表达状态，尊重减少动态效果的系统偏好。
- 用户可见文案使用自然中文，避免不必要的英文缩写和内部技术术语。
